program test_program is
    string s;
    int b[1:3];					// "int" is not defined for this language
begin
    if (s := "abc") then		// Cannot assign in if statement
       b[0] == 0;
       b[1] := 1;
     else
       b[3] := s;				// Cannot assign string to integer
     end if;
end program.
